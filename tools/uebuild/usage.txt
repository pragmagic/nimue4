Wrapper around Unreal Build Tool that compiles and prepares Nim files before
passing further to UBT

    uebuild [options]

Options:
  -d, --projectDir:PATH
  -e, --engineDir:PATH
  -p, --platform
  -t, --target
  -m, --mode
